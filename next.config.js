/** @type {import('next').NextConfig} */
const nextConfig = {
  // App directory is now stable in Next.js 14+
  // API routes handle their own body parsing via Request/Response APIs
}

module.exports = nextConfig
